# Read-only, disposable credential — never the Score Engine's DATABASE_URL.
TRAINING_DATABASE_URL=

# MLflow tracking/registry (same registry the Score Engine reads from).
MLFLOW_TRACKING_URI=
MLFLOW_MODEL_NAME=
